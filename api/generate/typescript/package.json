{
  "name": "@quizzit/types",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "clean": "rm -rf dist",
    "generate": "asyncapi generate models typescript ../../websocket-asyncapi.yaml -o dist",
    "ci": "npm run clean && npm run generate && npm publish && npm run clean"
  },
  "devDependencies": {
    "@asyncapi/cli": "^0.37.2",
    "@asyncapi/modelina": "^1.3.2"
  },
  "author": "",
  "license": "MIT"
}
